#### Integrations
##### Orange Cyberdefense
- Set minimum server version (fromVersion) to 6.0.0
- Lookup commands (Enrichment commands: `!ip`, `!domain`, etc. + Datalake specific lookup command: `!hashkey`)
  - Improve consistency of fields returned between "found" and "not found" response types
  - Added the following fields for backward compatibility: `found`, `status`, `datalake_url`
  - Added the full Datalake response data into the field `response_raw`